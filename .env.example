# GoHighLevel (server-side only, read in lib/ghl-client.ts)
GHL_API_TOKEN=
GHL_LOCATION_ID=

# Anthropic (server-side only, AI assistant)
ANTHROPIC_API_KEY=

# Dashboard login gate (server-side only)
# DASHBOARD_PASSWORD: the shared password you hand out.
# DASHBOARD_AUTH_SECRET: random string to sign the session cookie (openssl rand -hex 32).
DASHBOARD_PASSWORD=
DASHBOARD_AUTH_SECRET=
